"""Данные гейта покрытия: что учитываем и что проверено руками.

Отделено от `test_spec_coverage.py` намеренно. Код гейта не меняется часто,
а эти два множества правятся почти в каждом change'е и могут разрастаться до сотен
строк: держать их в одном файле с проверками — значит каждый раз читать диффом
данные вперемешку с логикой. Комментарии здесь несут не меньше смысла, чем
сами строки, поэтому формат — Python-модуль, а не JSON: в JSON объяснить,
почему сценарий закрыт распиской, негде.

Модуль называется не `test_*.py` сознательно: `marked()` в гейте ищет маркеры
регуляркой по файлам `test_*.py`, и строки из `MANUAL` засчитались бы как
покрытие тестом.
"""

from __future__ import annotations

# Capability, попавшие под учёт покрытия.
#
# `sales-plan-control` заархивирована — её сценарии держит гейт
# `test_archived_scenarios_are_covered`, и красный там означает, что change
# уехал в `openspec/specs/` с непроверяемым требованием.
#
# `sales-plan-create` внесена в момент появления дельта-спеки, задолго до
# архивации: до неё её сценарии — только строки отчёта. Внести позже значит
# не увидеть их вовсе, и об этом ничто не напомнит.
TRACKED: set[str] = {"sales-plan-control", "sales-plan-create"}

# Сценарии, проверенные не автотестом. Ключ — путь
# `<capability>/<Requirement>/<Scenario>` дословно, как в спеке;
# значение — путь к протоколу прогона относительно корня репозитория.
#
# Это расписка, а не исключение: гейт лишь следит, что сценарий и протокол
# существуют. Ценность записи — в комментарии над ней.
MANUAL: dict[str, str] = {
    # Читаемость экрана автотестом не берётся: проверяется не значение,
    # а то, что специалист видит нужное без прокрутки и фильтрации.
    "sales-plan-control/Единица измерения плана названа явно/Единица видна в рабочей таблице":
        "docs/sales-plan-control-check.md",
    "sales-plan-control/Ранжирование строк по вкладу в разрыв/Список приоритетов читается с экрана":
        "docs/sales-plan-control-check.md",
    # Сценарий «Незафиксированные правки не переживают перезагрузку» из
    # активного change'а `sales-plan-create` сюда ещё не внесён: протокол
    # `docs/sales-plan-create-check.md` не написан (tasks §5.4), а запись,
    # ссылающаяся на несуществующий файл, покраснит
    # test_manual_entries_are_valid. Расписка выдаётся после прогона,
    # а не вместо него.
}
